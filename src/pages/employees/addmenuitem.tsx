import { Grid } from '@mui/material';
import AddMenuItems from 'components/sections/dashboard/employee/AddMenuItems';
const addmenuitems = () => {
    return (
      <Grid container spacing={2}>
        <AddMenuItems />
      </Grid>
    );
  };
  
  export default addmenuitems;
  